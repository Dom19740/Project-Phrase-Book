import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.dpbcreative.travelchatter',
  appName: 'Travel Chatter',
  webDir: 'dist',
}

export default config
